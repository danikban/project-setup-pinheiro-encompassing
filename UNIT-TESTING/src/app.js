const add = (a = 0, b = 0) => a + b;
const sub = (a = 0, b = 0) => a - b;
const mul = (a = 0, b = 0) => a * b;
const div = (a = 0, b = 0) => a / b;
const sqrt = (a = 0) => Math.sqrt(a);

module.exports = {
  add,
  sub,
  mul,
  div,
};
